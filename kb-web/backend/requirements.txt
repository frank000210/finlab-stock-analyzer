fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
pymongo==4.7.3
PyJWT==2.8.0
requests==2.32.3
trafilatura==1.12.2
python-multipart==0.0.9
pytest==8.3.2
httpx==0.27.0
# Required so query-time embeddings use the same model as the stored
# semantic_vectors (paraphrase-multilingual-MiniLM-L12-v2, 384-dim).
# Without it the vendored knowledge_base package silently falls back to a
# 256-dim hashing pseudo-embedding, which is a different vector space --
# cosine similarity against real embeddings comes back near-zero and
# semantic search returns nothing (confirmed while testing /api/ask).
sentence-transformers>=5.0.0
